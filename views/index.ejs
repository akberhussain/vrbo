<html>
  <head>
    <title>VRBO</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="/stylesheets/main.css">
    <!-- <link rel="icon" href="/minkrealtylogo.png"> -->
<style type="text/css">
  
* {
  margin: 0;
  box-sizing: border-box;
}

body {
  font-family: "Roboto", sans-serif;
  /*background: #f7f6ff;*/
  text-decoration: none;
}



h2 {
  margin: 16px;
}

.mycontainerclass {
  margin-top: 10px;
  margin-left: 30%;
  padding: 32px;
}

.select-box {
  position: relative;
  display: flex;
  width: 400px;
  flex-direction: column;
}

.select-box .options-mycontainerclass {
  background: #2f3640;
  color: #f5f6fa;
  max-height: 0;
  width: 100%;
  opacity: 0;
  transition: all 0.4s;
  border-radius: 8px;
  overflow: hidden;

  order: 1;
}

.selected {
  background: #2f3640;
  border-radius: 8px;
  margin-bottom: 8px;
  color: #f5f6fa;
  position: relative;

  order: 0;
}

.selected::after {
  content: "";
  background: url("img/arrow-down.svg");
  background-size: contain;
  background-repeat: no-repeat;

  position: absolute;
  height: 100%;
  width: 32px;
  right: 10px;
  top: 5px;

  transition: all 0.4s;
}

.select-box .options-mycontainerclass.active {
  max-height: 240px;
  opacity: 1;
  overflow-y: scroll;
  margin-top: 54px;
}

a
{
  text-decoration: none;
  color: white;
}

.select-box .options-mycontainerclass.active + .selected::after {
  transform: rotateX(180deg);
  top: -6px;
}

.select-box .options-mycontainerclass::-webkit-scrollbar {
  width: 8px;
  background: #0d141f;
  border-radius: 0 8px 8px 0;
}

.select-box .options-mycontainerclass::-webkit-scrollbar-thumb {
  background: #525861;
  border-radius: 0 8px 8px 0;
}

.select-box .option,
.selected {
  padding: 12px 24px;
  cursor: pointer;
}

.select-box .option:hover {
  background: #414b57;
  color: white;
}

.select-box p {
  cursor: pointer;
}

.select-box .option .radio {
  display: none;
}

/* Searchbox */

.search-box input {
  width: 100%;
  padding: 12px 16px;
  font-family: "Roboto", sans-serif;
  font-size: 16px;
  position: absolute;
  border-radius: 8px 8px 0 0;
  z-index: 100;
  border: 8px solid #2f3640;

  opacity: 0;
  pointer-events: none;
  transition: all 0.4s;
}

.search-box input:focus {
  outline: none;
}

.select-box .options-mycontainerclass.active ~ .search-box input {
  opacity: 1;
  pointer-events: auto;
}


</style>
  
  </head>
  <body>
<!--<script type="text/javascript" src="https://code.jquery.com/jquery-3.2.1.min.js"></script>    -->

<!--<script type="text/javascript" src = "jquery-3.2.0.js"></script>-->
<script
  src="https://code.jquery.com/jquery-3.2.1.js"
  integrity="sha256-DZAnKJ/6XZ9si04Hgrsxu/8s717jcIzLy3oi35EouyE="
  crossorigin="anonymous"></script>


<script type="text/javascript" src = "https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>

  <nav class = "navbar navbar-default">
    <div class = "container-fluid">
        <div class = "navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bstarget" aria-expanded="false">
        		<span class="sr-only">Toggle navigation</span>
        		<span class="icon-bar"></span>
        		<span class="icon-bar"></span>
        		<span class="icon-bar"></span>
    		</button>
          
            <!-- <a class= "navbar-brand" href="/">Minkoffers</a> -->
        </div>
        <div class = "collapse navbar-collapse" id="bstarget">
          <div style="display:flex; justify-content: center; align-items: center; text-align: center;">
            <!-- <a href="/" style=""><img src="/minkrealtylogo.png" width="80px:" style="padding-top: 10%;"></a> -->
          </div>
          
          <div class = "nav navbar-nav" >
<!--           	<li><a href="/">Home</a></li>
            <li><a href="/">About</a></li> -->
          	<!-- <li><a href="https://www.minkrealty.com/" target = "_blank">Our Developers</a></li> -->
          	<!-- <li style="text-align: center; display: inline-block; margin-left: 50%;"></li> -->
            <% if(currentUser&&(currentUser[0].id=="123124")){ %>
<!--               <li><a href="/addproperty">Add property</a></li>
              <li><a href="/viewsellers">View all Offers</a></li>
              <li><a href="/view_all_users">View all Users</a></li> -->
            <% } %>
            
            <% if(currentUser&&(currentUser[0].id!="123124")){ %>
<!--               <li><a href="/view_offers/<%= currentUser[0].id %>">My Offers</a></li> -->
            <% } %>
    	
    		  </div>
<!--     		    <form class="navbar-form navbar-left" method = "post" action = "/results">
              <div class="form-group">
                <input type="text" class="form-control" placeholder="Search" name = "search">
              </div>
              <button type="submit" class="btn btn-default">Submit</button>
            </form> -->
            <ul class = "nav navbar-nav navbar-right">
             <% if(!currentUser){ %>
                  <li><a href="/login">Login</a></li>
                  <li><a href="/register">Sign Up</a></li>
                  <% } else { %>
                    <li><a href="#">Signed in as <%= currentUser[0].name %></a></li>
                    <li><a href="/logout">Logout</a></li>
                    
                <% } %>           
            </ul>
        </div>
    </div>
  </nav>
  
    <div class = "container">
      <% if (error && error.length > 0){ %>
        <div class = "alert alert-danger">
          <%= error %>
        </div>
      <% } %>
      <% if (success && success.length > 0){ %>      
        <div class = "alert alert-success">
          <%= success %>
        </div>
      <% } %>
    </div>


        <!-- Body -->

<div class = "container">
    <div class="container text-center">
      <!-- <h3>Agents, send offer or repair request to Donna Mink and seller at the same time</h3> -->
      <h2>Find your place for together</h2>
      <br>
<!--         <h4>Enter Property Below</h4>
        <h4>Enter Agent’s Email</h4>
        <h4>Enter few more details</h4>
        <h4>Upload offer and SEND</h4> -->

    </div>
    <div class="mycontainerclass">
      <div class="select-box">
        <div class="options-mycontainerclass">
          <% properties.forEach(function(property){ %>
          <a href="confirm/<%= property.id %>">
            <div class="option">
              <input type="radio" class="radio" id="<%= property.id %>" name="category" />
              <p for="<%= property.id %>"><%= property.name %> <%= property.city %> <%= property.state %></p>
            </div>
          </a>
          <% }); %>
        </div>

        <div class="selected">
          Enter "Street Number" or "Street Name"  
        </div>

        <div class="search-box">
          <input type="text" placeholder="Enter Street Number or Street Name" />
        </div>

      </div>
    </div>

</div>

<script type="text/javascript">

const selected = document.querySelector(".selected");
const optionsmycontainerclass = document.querySelector(".options-mycontainerclass");
const searchBox = document.querySelector(".search-box input");

const optionsList = document.querySelectorAll(".option");

selected.addEventListener("click", () => {
  optionsmycontainerclass.classList.toggle("active");

  searchBox.value = "";
  filterList("");

  if (optionsmycontainerclass.classList.contains("active")) {
    searchBox.focus();
  }
});

optionsList.forEach(o => {
  o.addEventListener("click", () => {
    selected.innerHTML = o.querySelector("p").innerHTML;
    optionsmycontainerclass.classList.remove("active");
  });
});

searchBox.addEventListener("keyup", function(e) {
  filterList(e.target.value);
});

const filterList = searchTerm => {
  searchTerm = searchTerm.toLowerCase();
  optionsList.forEach(option => {
    let p = option.firstElementChild.nextElementSibling.innerText.toLowerCase();
    if (p.indexOf(searchTerm) != -1) {
      option.style.display = "block";
    } else {
      option.style.display = "none";
    }
  });
};
  

</script>

<% include partials/footer %>